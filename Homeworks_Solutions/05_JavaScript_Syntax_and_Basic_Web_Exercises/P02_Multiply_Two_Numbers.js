function solve(arr){
    let numOne = Number(arr[0]);
    let numTwo = Number(arr[1]);

    console.log(numOne*numTwo);
    
}

solve(['2', '3']);
solve(['13', '13']);
solve(['1', '2']);
solve(['0', '50']);